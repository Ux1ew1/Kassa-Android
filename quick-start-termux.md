# Быстрый старт в Termux

## 1. Установка необходимых пакетов

```bash
pkg update && pkg upgrade
pkg install nodejs git
```

## 2. Переход в папку проекта

Если проект уже на телефоне:
```bash
cd ~/storage/downloads/Kassa-React
# или
cd ~/projects/Kassa-React
```

## 3. Быстрый запуск

### Вариант А: Использовать скрипт (рекомендуется)

```bash
chmod +x start-termux.sh
npm run termux
```

### Вариант Б: Ручной запуск

**Для продакшена (рекомендуется для мобильного):**
```bash
# Установка зависимостей (первый раз)
npm install

# Сборка (может занять 2-5 минут)
npm run build

# Запуск
npm start
```

**Для разработки (нужно два терминала):**
```bash
# Терминал 1
npm run server

# Терминал 2 (новое окно Termux)
npm run dev
```

## 4. Доступ к приложению

После запуска откройте браузер на телефоне:
- `http://localhost:3000` - если запущен в режиме продакшена

Для доступа с других устройств в той же WiFi:
- Найдите IP: `ifconfig | grep inet`
- Откройте `http://<IP>:3000` (например, `http://192.168.1.100:3000`)

## 5. Полезные команды

```bash
# Узнать IP адрес
ip addr show wlan0 | grep inet

# Остановить сервер
# Нажмите Ctrl+C в терминале

# Запустить в фоне (после сборки)
nohup npm start > server.log 2>&1 &

# Просмотр логов
tail -f server.log
```

## Важные замечания

1. ⚠️ **Производительность**: Сборка может занять время на мобильном устройстве
2. 🔋 **Батарея**: Приложение будет активно использовать CPU, зарядите телефон
3. 🔒 **Блокировка экрана**: Используйте `termux-wake-lock` чтобы Termux работал при блокировке
4. 📱 **Память**: Убедитесь, что достаточно свободного места (минимум 500MB)

